# CameraControl project context

Use `AGENTS.md` as the primary project instructions.

Read the relevant documents under `docs/` before changing camera,
ESP32, BLE, hardware, Tuya, networking, or persistence behavior.

The current repository code is the source of truth.